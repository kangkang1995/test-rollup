import React, { memo } from "react";
import { name } from "../../assets/utils";

const Test1 = () => {
    console.log("hello" + name);
    return (
        <div>1122365699996</div>
    );
};

export default memo(Test1);
